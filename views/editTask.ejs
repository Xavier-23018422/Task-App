<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <title>Task App</title>
</head>
<body>
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">Task App</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="collapsibleNavbar">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="/addTask">Add new task</a>
                    </li> 
                </ul>
            </div>
        </div>
    </nav>

    <div class="container">
        <br>
        <div class="text-center"><h2>Update Task</h2></div>
        <br>
        <div class="text-center">
            <% if (task) { %> 
                <form action="/editTask/<%= task.taskId %>" method="POST" enctype="multipart/form-data">
                    <label for="name">Task Name:</label><br>
                    <input type="text" id="name" name="name" value="<%= task.taskName %>" required><br><br>

                    <label for="description">Description:</label><br>
                    <input type="text" id="description" name="description" value="<%= task.description %>" required><br><br>

                    <label for="deadline">Deadline:</label><br>
                    <input type="date" id="deadline" name="deadline" value="<%= new Date(task.deadline).toISOString().split('T')[0] %>" required><br><br>
                    <span>(Current: <%= new Date(task.deadline).toLocaleDateString('en-GB') %>)</span><br><br>

                    <label for="image">Current Image:</label><br>
                    <img src="/images/<%= task.image %>" width="20%"><br><br>

                    <label for="newImage">New Image:</label><br>
                    <input type="file" id="image" name="image" accept="image/*"><br><br>

                    <button type="submit">Update Task</button>
                </form>
            <% } else { %>
                <p>No Task found.</p>
            <% } %>
            <a href="/">Back</a>
        </div>
    </div>
</body>
</html>

